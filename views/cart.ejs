<!DOCTYPE html>
<html lang="en">
<head>
    <link href="/css/cart.css" rel="stylesheet" >
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <%- include('./partials/head')%>
</head>

 <body>
    <%- include('./partials/header')%>
    <div class="wrapper">
        <h1>Tu orden:</h1>
        <div class="project">
            <div class="shop">
                <div class="box">
                    <img src="/images/<%= product.image%>" alt="<%= product.name %>" class="product-detail-img">
                    <div class="content">
                       <%= product.name %>
                       <p class="unit"> Cantidad: <input value="1"></p>

                       <p class="btn-area">
                           <i class="fa fa-trash"></i>
                           <span class="btn2"> Borrar </span>
                       </p>

                    </div>
                </div>
            </div>
            <div class="right-bar">
              <p><span>  <% let finalPrice = toThousand((product.price - (product.price * product.discount / 100)).toFixed(0));%>
                <% if (product.discount > 0) { 	%>
                    <!-- Si el producto tiene descuento, muestra lo de abajo-->
                    <p class="content">
                        <span><b><%= toThousand(product.price) %></b></span>
                        /
                        <b><%= product.discount %>% OFF</b>
                    </p>
                    <p class="content">
                         $ <%= finalPrice %>
                    </p>
                <% } else {  %>
                    <!-- si no, muestra el precio --> 
                    <p class="product-detail-price">
                           $<%= finalPrice %> 
                    </p>
                <% } %> </span></p>
                
                <a href="#"> <i class= "fa fa-shopping-cart"> Comprar </i></a>
            </div>
        </div>

    </div>
 

 </body>   
<!-- agrego el footer de clara-->
<br>
<footer>
    <%- include('./partials/footer')%>
</footer>

                        <!--<select name="cantidad" id="cantidad" class="btncantidad">
                            <option value="1">1</option>
                            <option value="2">2</option>
                            <option value="3">3</option>
                            <option value="4">4</option>
                            <option value="5">5</option>
                            <option value="6">6</option>
                            <option value="7">7</option>
                            <option value="8">8</option>
                            <option value="9">9</option>
                            <option >10</option>
                          </select> -->
</html>